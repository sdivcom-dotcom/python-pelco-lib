from setuptools import setup

setup(name='python_pelco_lib',
      version='0.1',
      description='Python PELCO library',
      packages=['python_pelco_lib'],
      author_email='sdivcom@yandex.ru',
      zip_safe=False)